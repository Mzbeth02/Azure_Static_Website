## 🎯 Deploy a Static Website to Azure Blob Storage using Bash and GitHub Actions

This project demonstrates how to **deploy a static website to Azure Blob Storage** using a **Bash script** and the **Azure CLI**.  
It also includes an optional **GitHub Actions workflow** to automate the deployment process whenever new files are pushed to the repository.

Check the documentation file for the deployment steps.
